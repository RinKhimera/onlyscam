/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "prestigious-donkey-523.convex.cloud",
      "images.unsplash.com",
      "res.cloudinary.com",
      "img.clerk.com",
      "files.edgestore.dev",
    ],
  },
}

export default nextConfig
