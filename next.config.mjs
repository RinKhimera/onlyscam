/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "prestigious-donkey-523.convex.cloud",
      },
    ],
  },
}

export default nextConfig
